# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment variables — never commit real keys
.env
.env.*
!.env.example

# Data files and local databases — never commit raw data
data/**
!data/**/
!data/**/.gitkeep
*.duckdb
*.duckdb.wal

# dbt profiles — contains local paths
dbt_airhealth/profiles.yml

# OS / editor
.DS_Store
.vscode/
.idea/
# Exception: commit the built warehouse so the deployed dashboard has data to query
!data/airhealth_usa.duckdb
# Jupyter autosave checkpoints — not real content
.ipynb_checkpoints/